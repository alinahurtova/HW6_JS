1.	Реализовать часы.
Реализовать отображение времени на экране.
Обновление данных должно быть в real time.
1.	Изначально время отображается в формате hh:mm:ss (full format)
2.	При клике по часам меняется формат отображения на hh:mm (short format).
3.	Реализовать toggle для отображения времени (“full/short” format).

2.	Реализовать функции sum через замыкание

function sum(x) {}
var sum1 = sum(1);
sum1(2);  

результат выполнения = 3

или вызов sum(1)(2); // 3

3.	В чем ошибка и почему?
var a = 5

(function() {
  alert(a);
})()

4.	В чем ошибка, почему и исправить код.

for (var i = 0; i < 10; i++) {
    setTimeout(function() {
        console.log(i);
    }, 0);
}

В итоге нужно вывести от 1 до 10.
